"""
store.py
─────────────────────────────────────────
애플리케이션 메모리 기반 저장소 모듈.

현재 저장 항목:
  - jwt_blocklist : 로그아웃된 JWT 토큰의 JTI(JWT ID) 집합
                    서버 재시작 시 초기화됩니다.
                    운영 환경에서는 Redis 같은 외부 캐시로 교체를 권장합니다.
"""

# ─── JWT 토큰 블랙리스트 ─────────────────────────
# 로그아웃 시 해당 토큰의 JTI를 이 집합에 추가하여 재사용을 차단합니다.
# JTI(JWT ID): 각 JWT 토큰에 부여되는 고유 식별자
jwt_blocklist: set = set()
